apiVersion: v1
kind: Service
metadata:
  name: chemuxer-mcp
  labels:
    app: chemuxer-mcp
spec:
  type: ClusterIP
  selector:
    app: chemuxer-mcp
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
