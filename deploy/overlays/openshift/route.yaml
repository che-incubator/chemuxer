apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: chemuxer-mcp
  labels:
    app: chemuxer-mcp
spec:
  to:
    kind: Service
    name: chemuxer-mcp
    weight: 100
  port:
    targetPort: 3001
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
