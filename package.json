{
  "name": "chemuxer-monorepo",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/chemuxer",
    "packages/chemuxer-mcp"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build --ws --if-present",
    "test": "npm test --ws --if-present",
    "lint": "npm run lint --ws --if-present"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
